# 企业级防篡改与反制 WAF - 部署使用说明书

## 一、 插件简介
本插件专为宝塔面板设计，深度融合了 Nginx Lua 高性能防护架构与 Python 后端管控。
特别针对 **V2Board, Xboard, ThinkPHP** 等框架提供了专属的防御层（App Shield），可有效拦截 SQL 注入、RCE 远程命令执行、高危目录爆破以及针对性文件篡改。

## 二、 如何在宝塔中安装
由于这是独立开发的第三方插件，无法直接从宝塔官方软件商店获取。请按以下步骤安装：

1. **打包插件**：
   在服务器命令行或本地电脑上，将 `bt-waf-plugin` 整个目录压缩为 `btwaf_v2board.zip`。
   > 注意：ZIP 包解压后的第一层目录应该是 `info.json`, `install.sh`, `index.html` 等文件，不要嵌套多层文件夹。

2. **导入宝塔**：
   - 登录宝塔面板。
   - 左侧导航栏点击 **软件商店**。
   - 点击右上角的 **第三方应用** 标签。
   - 点击 **导入插件**，上传刚才打包好的 `.zip` 文件。
   - 上传完成后，在列表中找到该插件，点击 **安装**。

## 三、 核心功能配置指南

### 1. 一键注入 Nginx 防护
打开插件面板，进入“安全防御”模块，点击红色的 **一键注入防护** 按钮。
- 插件会自动备份您当前的 Nginx 配置。
- 如果您的 Nginx 配置有语法错误，插件将**自动回滚**，保证业务不中断。

### 2. 开启 V2Board 专属防护
进入“框架适配”模块，目标框架选择 **Laravel / V2Board / Xboard**，点击保存。
这将会自动激活 `rules/v2board.rule`，深度屏蔽针对 `.env`、`storage/logs` 等敏感文件的探测。

### 3. 生成防篡改哈希基线
进入“防篡改巡检”模块：
- **保护目录**：输入您站点的真实路径，例如 `/www/wwwroot/v2board`。
- 点击 **生成/更新哈希基线**。插件会遍历所有核心代码文件，计算 SHA-256 指纹。
- **日常维护**：每周或怀疑被入侵时，点击 **执行完整性校验**，任何被修改过的 PHP 文件都将无所遁形。

## 四、 部署抗失联边缘网关 (高级篇)
当您的源站遭受极端 DDoS 攻击导致宝塔面板和 SSH 均无法登录时，您可以通过此功能下发急救指令（例如：硬封禁某个网段，或者开启全局 Tarpit 陷阱限流）。

1. 登录 Cloudflare，创建一个新的 Worker。
2. 将插件目录下的 `cloudflare_worker.js` 代码复制到 Worker 编辑器中。
3. 修改 Worker 代码顶部的配置项：
   - `ORIGIN_SERVER_URL`：修改为您源站的隐藏 API 接口。
   - `SECRET_TOKEN`：设置一个复杂的密钥。
4. 部署 Worker，并复制 Worker 的外网 URL。
5. 去 Telegram 申请一个 Bot，并将 Bot 的 Webhook 地址设置为刚才的 Worker URL。
6. 回到宝塔 WAF 插件的“双模反控”面板，填入 Token 和 Chat ID。

此时，您可以通过 Telegram 向机器人发送指令（如 `/waf ban 192.168.1.1`），Cloudflare 边缘节点将把指令加密，穿透拥堵的网络直达源站 Nginx 内核执行！
